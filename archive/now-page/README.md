# Archived `/now` page

Retired from the live site. The HTML snapshot here is for reference only; it is not linked or deployed. Public URLs `/now`, `/now/`, and `/now.html` redirect to `/`.

To restore: move `now.html` back to the repo root, re-add the route and nav entry, and remove the redirects in `server.js` and `vercel.json`.
