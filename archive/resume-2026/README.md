# Archived resume (2026-04)

The printable resume page is **not** linked on the public site. `resume.html` and `resume.md` live here for version history. Operators with `CLOUD_PASSWORD` can still use **`GET`/`PUT /api/resume`**, which reads and writes the HTML file in this folder.
